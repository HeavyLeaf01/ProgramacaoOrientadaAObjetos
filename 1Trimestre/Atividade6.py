class ClienteNovo:
    def __init__(self, c, n, i, e):
        self.CPF = c
        self.nome = n
        self.idade = i
        self.endereco = e
        self.brinde = []

        def addBrinde(self, b):
            if len(self.brindes) == 5:
                print("Chega, né? Fominha!")
            else:
                self.brindes += [b]

    def sortear():
        if len(listaBrindes) == 0:
            return False
        self.addbrinde = choice(listaBrindes)
            return True